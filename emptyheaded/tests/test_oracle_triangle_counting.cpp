#include "gtest/gtest.h"
#include "oracle_triangle_counting.cpp"

TEST(ORALCE_TRIANGLE_COUNTING, UINTEGER) {
  NUM_THREADS = 1;
  /*
  oracle_triangle_counting<uinteger> *myapp = new oracle_triangle_counting<uinteger>();
  myapp->run("tests/data/replicated.tsv");
  EXPECT_EQ((uint64_t)1612010, myapp->result);
  */
}